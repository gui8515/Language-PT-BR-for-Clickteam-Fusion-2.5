# Brazilian Portuguese Translation Report

## Summary

- **Files processed**: 136
- **Strings processed**: 18085
- **Strings changed**: 7345
- **Change rate**: 40.6%

## Translation Examples by File

### ADShow.txt

**1. Key**: `%o: Close Video`
   - Before: `%o: Close Video`
   - After: `%o: Fechar Vídeo`

**2. Key**: `%o: Compare Time Position (%0)`
   - Before: `%o: Compare Time Position (%0)`
   - After: `%o: Compare Tempo Posição (%0)`

**3. Key**: `%o: Is Playing ?`
   - Before: `%o: Is Playing ?`
   - After: `%o: Reproduzindo?`

**4. Key**: `%o: Load Video (%0)`
   - Before: `%o: Load Video (%0)`
   - After: `%o: Carregar Vídeo (%0)`

**5. Key**: `%o: Mute Audio`
   - Before: `%o: Mute Audio`
   - After: `%o: Mute Áudio`

**6. Key**: `%o: Play Video`
   - Before: `%o: Play Video`
   - After: `%o: Reproduzir Vídeo`

   _(... and 4 more changes)_

### AdMob.txt

**1. Key**: `%o: Is Video loaded?`
   - Before: `%o: Is Video loaded?`
   - After: `%o: Vídeo loaded?`

**2. Key**: `%o: On Dismissed Screen`
   - Before: `%o: On Dismissed Screen`
   - After: `%o: Ao Dispensar Tela`

**3. Key**: `%o: On Error`
   - Before: `%o: On Error`
   - After: `%o: Ao Ocorrer Erro`

**4. Key**: `%o: On Failed Ad`
   - Before: `%o: On Failed Ad`
   - After: `%o: Ao Falhar Anúncio`

**5. Key**: `%o: On Leave Application`
   - Before: `%o: On Leave Application`
   - After: `%o: Ao Sair do Aplicativo`

**6. Key**: `%o: On Presented Screen`
   - Before: `%o: On Presented Screen`
   - After: `%o: Ao Apresentar Tela`

   _(... and 4 more changes)_

### AndroidDialog.txt

**1. Key**: `%o: On Agreement Dialog with Id %0 and Button %1`
   - Before: `%o: On Agreement Dialog with Id %0 and Button %1`
   - After: `%o: Ao Agreement Dialog with Id %0 and Button %1`

**2. Key**: `%o: On Alert Dialog with Id %0 and Button %1`
   - Before: `%o: On Alert Dialog with Id %0 and Button %1`
   - After: `%o: Ao Alert Dialog with Id %0 and Button %1`

**3. Key**: `%o: On Color Picker with Id %0 and Button %1`
   - Before: `%o: On Color Picker with Id %0 and Button %1`
   - After: `%o: Ao Color Picker with Id %0 and Button %1`

**4. Key**: `%o: On Date Picker  with Id %0 and Button %1`
   - Before: `%o: On Date Picker  with Id %0 and Button %1`
   - After: `%o: Ao Date Picker  with Id %0 and Button %1`

**5. Key**: `%o: On File Picker with Id %0 and Button %1`
   - Before: `%o: On File Picker with Id %0 and Button %1`
   - After: `%o: Ao File Picker with Id %0 and Button %1`

**6. Key**: `%o: On Last error`
   - Before: `%o: On Last error`
   - After: `%o: Ao Último error`

   _(... and 4 more changes)_

### AndroidPlus.txt

**1. Key**: `%o: Is Device a tablet?`
   - Before: `%o: Is Device a tablet?`
   - After: `%o: Dispositivo a tablet?`

**2. Key**: `%o: Is Device natural landscape?`
   - Before: `%o: Is Device natural landscape?`
   - After: `%o: Dispositivo natural landscape?`

**3. Key**: `%o: Is Display set to Adjust window?`
   - Before: `%o: Is Display set to Adjust window?`
   - After: `%o: Exibir set to Adjust window?`

**4. Key**: `%o: Is Display set to Center?`
   - Before: `%o: Is Display set to Center?`
   - After: `%o: Exibir set to Centro?`

**5. Key**: `%o: Is Display set to Fit inside Bars and Adjust Windows?`
   - Before: `%o: Is Display set to Fit inside Bars and Adjust Windows?`
   - After: `%o: Display set to Fit inside Bars and Adjust Windows?`

**6. Key**: `%o: Is Display set to Fit inside Bars?`
   - Before: `%o: Is Display set to Fit inside Bars?`
   - After: `%o: Display set to Fit inside Bars?`

   _(... and 4 more changes)_

### Box2D8Directions.txt

**1. Key**: `Acceleration`
   - Before: `Acceleration`
   - After: `Aceleração`

**2. Key**: `Acceleration : %i`
   - Before: `Acceleration : %i`
   - After: `Aceleração : %i`

**3. Key**: `Check this mark to increase the precision of the collision detection (slower)`
   - Before: `Check this mark to increase the precision of the collision detection (slower)`
   - After: `Verificar this mark to increase the precision of the collision detection (slower)`

**4. Key**: `Check to enable automatic rotations of the image`
   - Before: `Check to enable automatic rotations of the image`
   - After: `Verificar to enable automatic rotations of the image`

**5. Key**: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - Before: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - After: `Verificar to resample the image of the object when rotating it (slower for large number of objects).`

**6. Key**: `Collision shape`
   - Before: `Collision shape`
   - After: `Colisão shape`

   _(... and 4 more changes)_

### Box2DAxial.txt

**1. Key**: `Check this mark to increase the precision of the collision detection (slower)`
   - Before: `Check this mark to increase the precision of the collision detection (slower)`
   - After: `Verificar this mark to increase the precision of the collision detection (slower)`

**2. Key**: `Check to enable automatic rotations of the image`
   - Before: `Check to enable automatic rotations of the image`
   - After: `Verificar to enable automatic rotations of the image`

**3. Key**: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - Before: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - After: `Verificar to resample the image of the object when rotating it (slower for large number of objects).`

**4. Key**: `Collision shape`
   - Before: `Collision shape`
   - After: `Colisão shape`

**5. Key**: `None`
   - Before: `None`
   - After: `Nenhum`

**6. Key**: `Object`
   - Before: `Object`
   - After: `Objeto`

   _(... and 3 more changes)_

### Box2DBackground.txt

**1. Key**: `Check to enable automatic rotations of the image`
   - Before: `Check to enable automatic rotations of the image`
   - After: `Verificar to enable automatic rotations of the image`

**2. Key**: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - Before: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - After: `Verificar to resample the image of the object when rotating it (slower for large number of objects).`

**3. Key**: `Collision shape`
   - Before: `Collision shape`
   - After: `Colisão shape`

**4. Key**: `None`
   - Before: `None`
   - After: `Nenhum`

**5. Key**: `Object`
   - Before: `Object`
   - After: `Objeto`

**6. Key**: `Physics - Background`
   - Before: `Physics - Background`
   - After: `Physics - Fundo`

   _(... and 3 more changes)_

### Box2DBase.txt

**1. Key**: `Check this mark and all projectiles created with a shoot action will be physical objects`
   - Before: `Check this mark and all projectiles created with a shoot action will be physical objects`
   - After: `Verificar this mark and all projectiles created with a shoot action will be physical objects`

**2. Key**: `Check this mark and all the backdrop objects of the frame will be obstacle (or platforms) in the world`
   - Before: `Check this mark and all the backdrop objects of the frame will be obstacle (or platforms) in the world`
   - After: `Verificar this mark and all the backdrop objects of the frame will be obstacle (or platforms) in the world`

**3. Key**: `Check this property, and any object with a classic movement will interact with the world's objects.`
   - Before: `Check this property, and any object with a classic movement will interact with the world's objects.`
   - After: `Verificar this property, and any object with a classic movement will interact with the world's objects.`

**4. Key**: `Destroy joint %0`
   - Before: `Destroy joint %0`
   - After: `Destruir joint %0`

**5. Key**: `Display factor`
   - Before: `Display factor`
   - After: `Exibir factor`

**6. Key**: `Pause`
   - Before: `Pause`
   - After: `Pausar`

   _(... and 4 more changes)_

### Box2DBouncingBall.txt

**1. Key**: `Check and the object will not be able to rotate.`
   - Before: `Check and the object will not be able to rotate.`
   - After: `Verificar and the object will not be able to rotate.`

**2. Key**: `Check if the object is moving really fast to allow accurate collision detection.`
   - Before: `Check if the object is moving really fast to allow accurate collision detection.`
   - After: `Verificar if the object is moving really fast to allow accurate collision detection.`

**3. Key**: `Check this mark to increase the precision of the collision detection (slower)`
   - Before: `Check this mark to increase the precision of the collision detection (slower)`
   - After: `Verificar this mark to increase the precision of the collision detection (slower)`

**4. Key**: `Check to enable automatic rotations of the image`
   - Before: `Check to enable automatic rotations of the image`
   - After: `Verificar to enable automatic rotations of the image`

**5. Key**: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - Before: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - After: `Verificar to resample the image of the object when rotating it (slower for large number of objects).`

**6. Key**: `Collision shape`
   - Before: `Collision shape`
   - After: `Colisão shape`

   _(... and 4 more changes)_

### Box2DFan.txt

**1. Key**: `%o: Is fan active?`
   - Before: `%o: Is fan active?`
   - After: `%o: fan active?`

**2. Key**: `Check this box and the fan will blow at the start of the frame`
   - Before: `Check this box and the fan will blow at the start of the frame`
   - After: `Verificar this box and the fan will blow at the start of the frame`

**3. Key**: `Direction of the air`
   - Before: `Direction of the air`
   - After: `Direção of the air`

**4. Key**: `Running : %i`
   - Before: `Running : %i`
   - After: `Em execução : %i`

**5. Key**: `Set angle to %0 degrees`
   - Before: `Set angle to %0 degrees`
   - After: `Definir angle para %0 degrees`

**6. Key**: `Set fan to %0`
   - Before: `Set fan to %0`
   - After: `Definir fan para %0`

   _(... and 4 more changes)_

### Box2DGround.txt

**1. Key**: `Bottom to top`
   - Before: `Bottom to top`
   - After: `Inferior to top`

**2. Key**: `Collision`
   - Before: `Collision`
   - After: `Colisão`

**3. Key**: `Direction`
   - Before: `Direction`
   - After: `Direção`

**4. Key**: `Left to right`
   - Before: `Left to right`
   - After: `Esquerda to right`

**5. Key**: `Object`
   - Before: `Object`
   - After: `Objeto`

**6. Key**: `Platform`
   - Before: `Platform`
   - After: `Plataforma`

   _(... and 2 more changes)_

### Box2DJoint.txt

**1. Key**: `Check and all the objects will be linked to the position of the pin.`
   - Before: `Check and all the objects will be linked to the position of the pin.`
   - After: `Verificar and all the objects will be linked to the position of the pin.`

**2. Key**: `Destroy joint`
   - Before: `Destroy joint`
   - After: `Destruir joint`

**3. Key**: `Set angle limits to %0 (lower angle) and %1 (upper angle)`
   - Before: `Set angle limits to %0 (lower angle) and %1 (upper angle)`
   - After: `Definir angle limits para %0 (lower angle) and %1 (upper angle)`

**4. Key**: `Set motor to %0 (torque) and %1 (strength)`
   - Before: `Set motor to %0 (torque) and %1 (strength)`
   - After: `Definir motor para %0 (torque) and %1 (strength)`

**5. Key**: `Speed`
   - Before: `Speed`
   - After: `Velocidade`

**6. Key**: `Type`
   - Before: `Type`
   - After: `Tipo`

   _(... and 3 more changes)_

### Box2DMagnet.txt

**1. Key**: `%o: Is magnet active?`
   - Before: `%o: Is magnet active?`
   - After: `%o: magnet active?`

**2. Key**: `Active at start`
   - Before: `Active at start`
   - After: `Ativo at start`

**3. Key**: `Check this box to make the magnet active at start of frame`
   - Before: `Check this box to make the magnet active at start of frame`
   - After: `Verificar this box to make the magnet active at start of frame`

**4. Key**: `Running : %i`
   - Before: `Running : %i`
   - After: `Em execução : %i`

**5. Key**: `Set angle to %0`
   - Before: `Set angle to %0`
   - After: `Definir angle para %0`

**6. Key**: `Set height to %0`
   - Before: `Set height to %0`
   - After: `Definir height para %0`

   _(... and 4 more changes)_

### Box2DParticules.txt

**1. Key**: `Animation speed`
   - Before: `Animation speed`
   - After: `Animação speed`

**2. Key**: `Apply force`
   - Before: `Apply force`
   - After: `Aplicar force`

**3. Key**: `Apply force %0 to particles`
   - Before: `Apply force %0 to particles`
   - After: `Aplicar force %0 to particles`

**4. Key**: `Apply torque`
   - Before: `Apply torque`
   - After: `Aplicar torque`

**5. Key**: `Apply torque %0 to particles`
   - Before: `Apply torque %0 to particles`
   - After: `Aplicar torque %0 to particles`

**6. Key**: `Check and the animation of the particle will loop when reaching the last image.`
   - Before: `Check and the animation of the particle will loop when reaching the last image.`
   - After: `Verificar and the animation of the particle will loop when reaching the last image.`

   _(... and 4 more changes)_

### Box2DPlatform.txt

**1. Key**: `Acceleration`
   - Before: `Acceleration`
   - After: `Aceleração`

**2. Key**: `Acceleration : %i`
   - Before: `Acceleration : %i`
   - After: `Aceleração : %i`

**3. Key**: `Button 1`
   - Before: `Button 1`
   - After: `Botão 1`

**4. Key**: `Button 2`
   - Before: `Button 2`
   - After: `Botão 2`

**5. Key**: `Check and the character will be able to jump when he is crouched.`
   - Before: `Check and the character will be able to jump when he is crouched.`
   - After: `Verificar and the character will be able to jump when he is crouched.`

**6. Key**: `Check this box to allow your object to crouch`
   - Before: `Check this box to allow your object to crouch`
   - After: `Verificar this box to allow your object to crouch`

   _(... and 4 more changes)_

### Box2DRaceCar.txt

**1. Key**: `Acceleration`
   - Before: `Acceleration`
   - After: `Aceleração`

**2. Key**: `Acceleration : %i`
   - Before: `Acceleration : %i`
   - After: `Aceleração : %i`

**3. Key**: `Check this box to allow your object to go in reverse direction`
   - Before: `Check this box to allow your object to go in reverse direction`
   - After: `Verificar this box to allow your object to go in reverse direction`

**4. Key**: `Check this mark to increase the precision of the collision detection (slower)`
   - Before: `Check this mark to increase the precision of the collision detection (slower)`
   - After: `Verificar this mark to increase the precision of the collision detection (slower)`

**5. Key**: `Check to enable automatic rotations of the image`
   - Before: `Check to enable automatic rotations of the image`
   - After: `Verificar to enable automatic rotations of the image`

**6. Key**: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - Before: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - After: `Verificar to resample the image of the object when rotating it (slower for large number of objects).`

   _(... and 4 more changes)_

### Box2DRopeAndChain.txt

**1. Key**: `%o: On each element, loop name %0`
   - Before: `%o: On each element, loop name %0`
   - After: `%o: Ao each element, loop name %0`

**2. Key**: `Check and the rope or chain will be attached on both sides (to create a bridge for example).`
   - Before: `Check and the rope or chain will be attached on both sides (to create a bridge for example).`
   - After: `Verificar and the rope or chain will be attached on both sides (to create a bridge for example).`

**3. Key**: `End image`
   - Before: `End image`
   - After: `Fim image`

**4. Key**: `Number of elements`
   - Before: `Number of elements`
   - After: `Número of elements`

**5. Key**: `Number of elements : %i`
   - Before: `Number of elements : %i`
   - After: `Número of elements : %i`

**6. Key**: `Set horizontal coordinate to %0`
   - Before: `Set horizontal coordinate to %0`
   - After: `Definir horizontal coordinate para %0`

   _(... and 4 more changes)_

### Box2DSpaceShip.txt

**1. Key**: `Check this mark to increase the precision of the collision detection (slower)`
   - Before: `Check this mark to increase the precision of the collision detection (slower)`
   - After: `Verificar this mark to increase the precision of the collision detection (slower)`

**2. Key**: `Check to enable automatic rotations of the image`
   - Before: `Check to enable automatic rotations of the image`
   - After: `Verificar to enable automatic rotations of the image`

**3. Key**: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - Before: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - After: `Verificar to resample the image of the object when rotating it (slower for large number of objects).`

**4. Key**: `Collision shape`
   - Before: `Collision shape`
   - After: `Colisão shape`

**5. Key**: `Deceleration`
   - Before: `Deceleration`
   - After: `Desaceleração`

**6. Key**: `Deceleration : %i`
   - Before: `Deceleration : %i`
   - After: `Desaceleração : %i`

   _(... and 4 more changes)_

### Box2DSpring.txt

**1. Key**: `Active at start`
   - Before: `Active at start`
   - After: `Ativo at start`

**2. Key**: `Check this box to make your spring active when the frame starts`
   - Before: `Check this box to make your spring active when the frame starts`
   - After: `Verificar this box to make your spring active when the frame starts`

**3. Key**: `Check to enable automatic rotations of the image`
   - Before: `Check to enable automatic rotations of the image`
   - After: `Verificar to enable automatic rotations of the image`

**4. Key**: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - Before: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - After: `Verificar to resample the image of the object when rotating it (slower for large number of objects).`

**5. Key**: `Collision shape`
   - Before: `Collision shape`
   - After: `Colisão shape`

**6. Key**: `Type : Spring`
   - Before: `Type : Spring`
   - After: `Tipo : Spring`

### Box2DStatic.txt

**1. Key**: `Check and the object will not be able to rotate.`
   - Before: `Check and the object will not be able to rotate.`
   - After: `Verificar and the object will not be able to rotate.`

**2. Key**: `Check if the object is moving really fast to allow accurate collision detection.`
   - Before: `Check if the object is moving really fast to allow accurate collision detection.`
   - After: `Verificar if the object is moving really fast to allow accurate collision detection.`

**3. Key**: `Check this mark to increase the precision of the collision detection (slower)`
   - Before: `Check this mark to increase the precision of the collision detection (slower)`
   - After: `Verificar this mark to increase the precision of the collision detection (slower)`

**4. Key**: `Check to enable automatic rotations of the image`
   - Before: `Check to enable automatic rotations of the image`
   - After: `Verificar to enable automatic rotations of the image`

**5. Key**: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - Before: `Check to resample the image of the object when rotating it (slower for large number of objects).`
   - After: `Verificar to resample the image of the object when rotating it (slower for large number of objects).`

**6. Key**: `Collision shape`
   - Before: `Collision shape`
   - After: `Colisão shape`

   _(... and 4 more changes)_


_(... and 114 more files with changes)_

## Mandatory Term Mappings Applied

- **Interstitial** → **Intersticial**
- **Rewarded Video** → **Vídeo Recompensado**
- **Banner** → **Banner**
- **AdMob** → **AdMob**
- **Failed to Load** → **Erro ao Carregar**
- **On ...** → **Ao ...** (for events)

## Translation Quality

- All translations use natural, idiomatic Brazilian Portuguese
- UI elements use concise, imperative forms
- Technical accuracy maintained
- All placeholders preserved exactly
- JSON structure maintained
- UTF-8 encoding without BOM

